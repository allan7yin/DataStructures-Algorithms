#include "../bits/stdc++.h"
using namespace std;

/**
 * Idea is, power of is 10...0 so minus 1 will be 0111.111
 * So, n & (n-1) will be 0 if n is power of 2
 */
class Solution {
  public:
    bool isPowerOfTwo(int n) {
        return
    }
};