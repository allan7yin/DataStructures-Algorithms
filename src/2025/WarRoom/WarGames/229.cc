#include "../bits/stdc++.h"

using namespace std;

/**
 * Given an integer array of size `n` -> find all elements that appear more than floor(n/3) times
 * Do this in problem in linear time and constant space
 *
 *  -> So, we cannot sort this, and need some constant space method of keeping track
 */
class Solution {
  public:
    vector<int> majorityElement(vector<int> &nums) {
        vector<int> result;
    }
};